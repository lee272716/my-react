import logo from './logo.svg';
import './App.css';
import { useState, useReducer, useTransition, startTransition, useDeferredValue, useRef, useImperativeHandle } from 'react';

import TaskList from './TaskList.js';

function App() {
  return (
    <div className="App">
      useState usetReducer useEffect useLayoutEffect useMemo useCallback useRef useImperativeHandle useDeferredValue useTransition startTransition
      <h1>测试本地代码库提交到远程仓库</h1>
    </div>
  );
}

export default App;
